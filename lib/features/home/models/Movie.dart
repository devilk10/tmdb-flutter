class Movie {
  int id;
  String title;
  String overview;
  String posterPath;
  double voteAverage;

  Movie({
    required this.id,
    required this.title,
    required this.overview,
    required this.posterPath,
    required this.voteAverage,
  });
}
